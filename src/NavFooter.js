const NavFooter = ({ React }) => (
  <div className="topNav">
    <nav className="navBar">
      <a href="#">Contacto</a>
      <a href="#">Produtos</a>
      <a href="#">Inicio</a>
    </nav>
  </div>
);

export default NavFooter;
